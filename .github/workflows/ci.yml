name: V-CORE-CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Check Syntax
        run: |
          python3 -m py_compile *.py
          echo "Синтаксис стабилен."
